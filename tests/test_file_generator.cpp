#include <gtest/gtest.h>

// Simple sanity test to verify the test harness works.
// Extend this file with real unit tests that include project headers.

TEST(Sanity, BasicMath) {
    EXPECT_EQ(1 + 1, 2);
}

// Placeholder for a real test example:
//
// #include "your_header.h"
// TEST(FileGenerator, GeneratesExpectedHeader) {
//    FileGenerator fg(...);
//    auto out = fg.generate(...);
//    EXPECT_EQ(out.some_field, expected_value);
//}
