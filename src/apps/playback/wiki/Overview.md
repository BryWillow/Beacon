# Overview

Exchange Market Data Playback replays binary market data files with configurable timing, burst, wave, and chaos modes.  
It is modular, with senders, authorities (rules), and advisors (classifiers) configured via JSON.

See [Configuration Guide](Configuration.md) for details.
